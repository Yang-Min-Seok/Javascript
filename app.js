// const -> 이후에 바꿀 예정이 없는 경우 사용
const a = 5;
const b = 2;

// let -> 이후에 바꿀 예정이 있는 경우 사용
let myName = 'kurooru';
myName = 'tamama';

// 그러므로, 기본적으로 const사용 후 필요할 때만 let 사용하는쪽으로 코드 짜기
// Always const sometimes let never var.