const title = document.getElementById('title');
// brding every information from Id
console.dir(title);
// innerText changes
title.innerText='Got you!';

console.log(title.id);
console.log(title.className);